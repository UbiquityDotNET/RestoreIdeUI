version: 1.0.{build}
image: Visual Studio 2019

configuration: Release
platform: Any CPU

before_build:
- cmd: msbuild RestoreIdeUI.sln /restore

build:
  project: RestoreIdeUI.sln
  verbosity: minimal
  
artifacts:
- path: ./BuildOutput/VSIX/*.vsix
  name: VSIX Package
